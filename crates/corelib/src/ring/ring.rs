//! Hash ring data structure (placeholder).
//!
//! Will hold `BTreeMap<Token, NodeId>` and lookup logic.

/// Placeholder for the main hash ring type.
#[derive(Debug)]
pub struct HashRing;

/// Placeholder for building a ring (builder pattern).
#[derive(Debug)]
pub struct RingBuilder;
