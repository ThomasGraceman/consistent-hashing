//! Ring topology abstractions.
//!
//! This module will contain high-level views over the ring such as
//! routing, ownership calculation, and range queries.

/// Placeholder for ring topology / routing (ownership, ranges).
#[derive(Debug, Clone)]
pub struct Topology;
